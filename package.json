{
  "name": "lossless-audio-relay",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "description": "Cross-platform lossless realtime audio relay for NATed clients.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @lossless-audio/protocol && npm run build -w @lossless-audio/server && npm run build -w @lossless-audio/desktop",
    "test": "npm run test -ws --if-present && npm run test:sonobus-relay-state",
    "test:macos-bundle-identity": "node tools/check-macos-bundle-identity.mjs",
    "test:sonobus-relay-state": "node tools/check-sonobus-relay-state.mjs",
    "dev:server": "npm run dev -w @lossless-audio/server",
    "dev:desktop": "npm run dev -w @lossless-audio/desktop",
    "typecheck": "npm run typecheck -ws --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}

# SonoBus 无损音频中继项目接手说明

更新时间：2026-09-09

## 1. 项目位置

迁移后的项目目录：

~~~
/Volumes/存储/Applications/ls/SonoBus无损音频中继
~~~

这是完整 Git 工作区，包含 SonoBus 源码、Web/服务器、Windows MoLiXiu 桥接代码、构建脚本、未提交的构建产物和当前工作分支。

远程仓库：

~~~
https://github.com/xinzhihong-ship-it/lossless-audio-sonobus-relay-private.git
~~~

当前分支和版本基线：

~~~
分支：codex/dshow-drainer
最新源码提交：63c1fae Cover YYAnchor bridge classifier fixture
本交接文档随后的独立提交仅更新验收记录，不改变源码基线
已部署验证基线：00ff309（非 ASIO，v0.1.32）
标签：v0.1.32
~~~

不要把服务器密码、SSH 私钥、后台密码或其他凭据写入本文件、Issue、截图或公开仓库。

## 2. 当前已经完成

### 客户端与插件

- SonoBus 客户端支持 Windows、macOS、Linux。
- 已保留 Standalone、VST3、AU、LV2 等原有插件能力。
- Windows ASIO 构建流程会同时生成客户端、VST3、FFmpeg、SonoBusVideoCaptureHelper.exe 和 32 位 MoLiXiu 桥接组件。
- Windows 安装包会把 SonoBusMoLiXiuBridge.exe 和 SonoBusMoLiXiuHook.dll 放入客户端目录以及两个 VST3 目录。

### MoLiXiu 摄像头链路

核心代码位置：

~~~
sonobus/Source/WindowsVideoCaptureHelper.cpp
tools/windows-molixiu-bridge/SonoBusMoLiXiuBridge.cpp
tools/windows-molixiu-bridge/SonoBusMoLiXiuHook.cpp
tools/windows-molixiu-bridge/SonoBusMoLiXiuFrame.h
~~~

当前设计是：

1. 32 位桥接程序发现 molixiu.exe，向其注入 SonoBusMoLiXiuHook.dll。
2. Hook 读取魔力秀内部当前选择的画面，并写入命名共享内存。
3. Hook/桥接程序把当前设备标识写到 %APPDATA%\\SonoBus\\molixiu-camera.txt。
4. SonoBus helper 优先读取 Hook 共享帧。
5. Hook 暂停或魔力秀关闭画面时，helper 根据最近一次设备标识尝试共享读取或短时 DirectShow fallback。
6. fallback 会定时释放摄像头，避免长期独占，让魔力秀和其他软件重新取得摄像头。
7. Hook 恢复后立即回到 Hook 帧，Hook 帧优先于 fallback。

相关实现提交：

~~~
6c891d9 Keep streaming MoLiXiu's selected camera when its hook feed stops
e1428e6 Declare spawnChildProcess before its first caller
724e5df Keep YYAnchor cameras on DirectShow path
63c1fae Cover YYAnchor bridge classifier fixture
~~~

### 2026-09-09 YYAnchor 诊断结论

- 测试机当前可见的实际进程包括 `YYAnchor.exe`、`yyobs.exe` 和 SonoBusVideoCaptureHelper；可见模块查询报告 `SonoBusMoLiXiuHook.dll` 与 `molixiudll.dll` 均为 0 个匹配。该证据支持“当前生产者是 YYAnchor，而不是已确认兼容的 `molixiu.exe`”，但仍不能据此宣称 Hook 全面状态已完成验收。
- `SonoBusMoLiXiuBridge.exe` 的实现仍只寻找 `molixiu.exe`，并依赖固定的 MoLiXiu PE 布局、ABI 和 32 位注入；禁止仅把 `YYAnchor.exe` 加入目标列表。
- `dshow:@device_sw_{860BB310-5D01-11D0-BD3B-00A0C911CE86}\\yyanchorvcam` 已确认属于已知虚拟 DirectShow 设备。`63c1fae` 将 YYAnchor 的三个已知标识（`yyanchorvcam`、`yyanchormulvcam`、`yyanchormulcam`）与私有 MoLiXiu Hook 路由分开，避免仅因显示名含“魔力秀/YY开播”而改成 `--publish-molixiu`。
- `00ff309` 安装包仍是测试机当前部署版本；`63c1fae` 尚未生成完整 Windows 产物、尚未部署，也尚未完成 Windows 真机回归。不得把本地单元测试结果当作远端部署完成证据。
- 最新一次可见 DirectShow 探测已进入 real-time buffer/frame dropped 路径，而不是此前的 `Could not find output pin`；这只证明探测边界有变化，仍需在新构建的直接 DirectShow 路由下确认持续递增帧和 RTSP 输出。


Hook 目前针对魔力秀 2.0.2111.2402 的固定二进制布局，并且故意对其他版本 fail closed。魔力秀升级后，需要重新确认时间戳、镜像大小、共享数据 RVA、回调偏移和像素结构。

### 服务端视频链路

- 客户端上传 H.264，服务器不重新编码，只做中转和 MediaMTX/RTSP/WebRTC 输出。
- 已增加 RTSP demuxer 超时、输入停滞/输出断开后的 muxer 重启，以及 muxer stdin EPIPE 处理。
- 相关提交：

~~~
a2b6b8a Pass client H264 through server muxer without re-encoding
b2cc2fc Restart group muxer when ingest stalls or output dies
fffcd30 Use RTSP demuxer socket timeout and survive muxer stdin EPIPE
~~~

服务器和后台的常规部署说明在：

~~~
docs/deployment.md
docs/video-relay.md
docs/download-and-use.md
~~~

## 3. 当前未完成/需要接手继续验证

当前最大的未闭环问题是：测试机已直接部署并核验非 ASIO `00ff309` v0.1.32 payload，但最新的 `63c1fae` YYAnchor 路由修复尚未进入 Windows 产物，也没有完成真机持续帧、RTSP/WebRTC、OBS、REAPER/VST3 和生命周期验收。不要把旧部署的结果归因于新提交；本轮重新部署仍需完整 artifact 和明确授权。

按以下顺序处理：

### A. 为 63c1fae 生成并授权 Windows 真机部署

测试机：Windows 远程测试机 WIN-CUG15J16S85。

当前安装目录仍以已核验的 `00ff309` 非 ASIO payload 为基线。只有在 `63c1fae` 的 ASIO/非 ASIO 完整产物、manifest/hash 和测试均通过，并取得明确部署授权后，才可替换它。替换前关闭：

- REAPER/其他 DAW
- SonoBus helper
- 旧版 ffmpeg.exe
- 旧版 SonoBusMoLiXiuBridge.exe

安装后检查以下两个 VST3 目录都存在新版文件：

~~~
C:\Program Files\Common Files\VST3\SonoBus.vst3\Contents\x86_64-win\
C:\Program Files\Common Files\VST3\SonoBusInstrument.vst3\Contents\x86_64-win\
~~~

至少确认：

~~~
SonoBusMoLiXiuBridge.exe
SonoBusMoLiXiuHook.dll
ffmpeg.exe
SonoBusVideoCaptureHelper.exe
~~~

验证新版桥不再接受旧的假成功行为：

~~~
& 'C:/Program Files/Common Files/VST3/SonoBusInstrument.vst3/Contents/x86_64-win/SonoBusMoLiXiuBridge.exe' --attach --dll 'C:/Windows/definitely-missing.dll'
$LASTEXITCODE
~~~

结果应为非零；旧版桥曾经对这个错误参数返回 0。

### B. 确认 Hook 真正注入

启动魔力秀和 DAW/VST 后检查：

~~~
tasklist /m SonoBusMoLiXiuHook.dll
Get-Content "$env:APPDATA\SonoBus\molixiu-camera.txt"
~~~

必须能看到 Hook 已加载到 molixiu.exe，并且设备文件内容会随魔力秀选择摄像头变化。设备名是用户可自定义的，不能在代码里写死 YY开播等显示名称，应使用设备标识/最近选择快照。

### C. 做真正的断开/重开验收

在魔力秀中依次测试：

1. 选择物理摄像头，确认 VST/后台/RTSP 有真实运动画面。
2. 关闭魔力秀摄像头，确认 SonoBus 仍能从最近选择的设备取得画面，或者在设备确实不可读时明确报告无帧。
3. 再次打开魔力秀摄像头，确认画面自动恢复，不需要重启 DAW。
4. 切换到 YY/其他虚拟摄像头，再切回物理摄像头。
5. 魔力秀播放视频文件、暂停视频、切回摄像头，确认魔力秀自己的输出不被抢占。
6. 同时让其他软件打开同一摄像头，确认 SonoBus fallback 会释放设备，不持续独占。

观察 helper 输出中的状态：

~~~
capture_mode=molixiu-hook
capture_mode=molixiu-direct
molixiu_source=hook
~~~

若只出现“等待魔力秀选择摄像头”，先查 Hook 是否加载和 molixiu-camera.txt 是否更新，不要先改服务器。

### D. 验证服务器实际有帧

客户端画面正常后，在服务器或能访问服务器的机器上用 FFmpeg 拉取 RTSP，至少持续 15–30 秒并确认帧数大于 0：

~~~
& 'C:/Program Files/Lossless Audio SonoBus Relay/ffmpeg.exe' -hide_banner -loglevel warning -rtsp_transport tcp -analyzeduration 10M -probesize 10M -i 'rtsp://<服务器地址>:19092/SB_<群组名>' -t 15 -map 0:v:0 -an -f null NUL
~~~

同时验证浏览器 /SB_<群组名> 和 OBS 的 RTSP 地址。若 FFmpeg 有帧而网页/OBS 无画面，再查 MediaMTX/WebRTC/浏览器；若 FFmpeg 也无帧，先查客户端 helper 日志和上行连接。

### E. 最后重新构建和发布（针对 63c1fae）

本地基础检查：

~~~
npm ci
npm run typecheck
npm test
npm run build
~~~

Windows 原生桥接和安装包必须通过 GitHub Actions：

~~~
.github/workflows/build-windows-asio.yml
.github/workflows/build-windows.yml
~~~

发布前核对：Actions 成功、tag 指向正确提交、Release 为 published、Windows ASIO 安装包非空且包含上述四类文件。不要只以“源码编译成功”作为完成依据。

## 4. 关键约束

- 不得独占物理摄像头或虚拟摄像头。
- fallback 只能短时读取，并且必须释放；其他软件和魔力秀应能重新取得设备。
- 不得因为魔力秀的显示名称变化而失效。
- 不要把视频重新编码当成默认修复方案；服务器目标是 H.264 中转。
- 不要静默把用户切换到另一台摄像头；设备不可读时要保留错误状态和日志。
- 不要把真实服务器密码、SSH 私钥、后台账号密码提交到 Git。

## 5. 接手时第一步

~~~
cd "/Volumes/存储/Applications/ls/SonoBus无损音频中继"
git status --short --branch
git log -5 --oneline
~~~

先确认 `00ff309` 当前部署基线与 `63c1fae` 新产物严格区分；完成两套 Windows CI、artifact provenance/hash 和真机路由验收后，才允许替换测试机安装。否则很容易把旧安装包问题误判成新代码问题。


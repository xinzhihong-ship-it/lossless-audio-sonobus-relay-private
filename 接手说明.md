# SonoBus 无损音频中继项目接手说明

## 最新状态（优先于下方历史记录）

- 当前 HEAD 实查为 `2de49cd Fix mixed-case DirectShow selector matching`；分支 `codex/dshow-drainer` 已与 `origin/codex/dshow-drainer` 同步；源码 tracked 改动已提交，工作区只保留既有未跟踪产物和本说明的后续记录更新。
- `f79d866` 已包含路由与发布尝试修复：所有相关 `dshow:` 判断改为大小写不敏感；`isMoLiXiuBridgeCamera()` 在名称启发式之前拒绝所有 `dshow:` ID；MoLiXiu 选择只使用 `molixiu-hook`，不再自动切换到未授权的 YYAnchor、OBS 或 WebcastMate；DirectShow/虚拟设备启动尝试在模式查询前记录，失败后相同管理员状态不会因 `desiredChanged` 再次打开。`2de49cd` 将完整 DirectShow selector 的匹配、设备合并、实际采集 selector 和管理页选择统一为大小写不敏感但内容精确。
- `2de49cd` 已让完整 DirectShow selector 仅大小写不敏感、仍要求精确匹配；统一设备查找、x64/x86 合并、FFmpeg 路径、虚拟分类、实际 `-i` selector 和管理页选择逻辑，并覆盖 C++ 与管理页 HTML 回归测试。
- 未修改 `isKnownVirtualCamera()` 的识别规则；未把防护延后到 publisher 参数阶段；未按 `molixiu.exe` 进程存在而停止管理员明确选择的虚拟设备。
- 本次接手后验证：`SonoBusVideoTests`/CTest `1/1` 通过；macOS `SonoBus_Standalone` 和 `SonoBus_VST3` 编译通过；`npm run typecheck`、`npm test` `42/42`、`npm run build` 和 `git diff --check` 全部通过。macOS libc++ 输出的兼容性信息仍只是已有警告。
- 针对 `2de49cd` 重新运行的非 ASIO/ASIO Windows Actions 已成功：非 ASIO `34609121870`、ASIO `34609127561`，两者 `headSha` 均为 `2de49cdcfe59e0dae70cc29b4a162f532e3a5fab`。新的 portable ZIP、manifest/hash/layout 和 Windows 原生 installer payload 验证均已通过；artifact 仅下载到 `/tmp/sonobus-artifacts-2de49cd-*`，未写入仓库。测试机仍保持已核验的非 ASIO `00ff309`，没有部署当前 artifact；真机持续帧及 RTSP/WebRTC/OBS/REAPER/VST3 验收仍未完成。

更新时间：2026-09-11（本次接手核对）

## 1. 项目位置

迁移后的项目目录：

~~~
/Volumes/存储/Applications/ls/SonoBus无损音频中继
~~~

这是完整 Git 工作区，包含 SonoBus 源码、Web/服务器、Windows MoLiXiu 桥接代码、构建脚本、未提交的构建产物和当前工作分支。

远程仓库：

~~~
https://github.com/xinzhihong-ship-it/lossless-audio-sonobus-relay-private.git
~~~

当前分支和版本基线：

~~~
分支：codex/dshow-drainer
最新源码提交：2de49cd Fix mixed-case DirectShow selector matching
功能基线：63c1fae Cover YYAnchor bridge classifier fixture；f79d866 完成本轮路由与发布尝试修复，2de49cd 完成混合大小写 selector 修复并已推送到远端分支
本交接文档的记录更新不改变上述源码基线；当前 selector 修复及相关文档/测试改动尚未形成新提交
已部署验证基线：00ff309（非 ASIO，v0.1.32）
标签：v0.1.32
~~~

不要把服务器密码、SSH 私钥、后台密码或其他凭据写入本文件、Issue、截图或公开仓库。

## 2. 当前已经完成

### 客户端与插件

- SonoBus 客户端支持 Windows、macOS、Linux。
- 已保留 Standalone、VST3、AU、LV2 等原有插件能力。
- Windows ASIO 构建流程会同时生成客户端、VST3、FFmpeg、SonoBusVideoCaptureHelper.exe 和 32 位 MoLiXiu 桥接组件。
- Windows 安装包会把 SonoBusMoLiXiuBridge.exe 和 SonoBusMoLiXiuHook.dll 放入客户端目录以及两个 VST3 目录。

### MoLiXiu 摄像头链路

核心代码位置：

~~~
sonobus/Source/WindowsVideoCaptureHelper.cpp
tools/windows-molixiu-bridge/SonoBusMoLiXiuBridge.cpp
tools/windows-molixiu-bridge/SonoBusMoLiXiuHook.cpp
tools/windows-molixiu-bridge/SonoBusMoLiXiuFrame.h
~~~

当前设计是：

1. 32 位桥接程序发现 molixiu.exe，向其注入 SonoBusMoLiXiuHook.dll。
2. Hook 读取魔力秀内部当前选择的画面，并写入命名共享内存。
3. Hook/桥接程序把当前设备标识写到 %APPDATA%\\SonoBus\\molixiu-camera.txt。
4. SonoBus helper 优先读取 Hook 共享帧。
5. Hook 暂停或魔力秀关闭画面时，helper 根据最近一次物理设备标识尝试 `SharedReadOnly` fallback；不会通过 DirectShow 兜底。
6. SharedReadOnly fallback 会定时释放摄像头，让魔力秀和其他需要独占的程序重新取得设备。
7. Hook 恢复且当前 fallback 已释放后，回到 Hook 帧；虚拟摄像头不会自动作为 fallback。

相关实现提交：

~~~
6c891d9 Keep streaming MoLiXiu's selected camera when its hook feed stops
e1428e6 Declare spawnChildProcess before its first caller
724e5df Keep YYAnchor cameras on DirectShow path
63c1fae Cover YYAnchor bridge classifier fixture
~~~

### 2026-09-09 YYAnchor 诊断结论

- 测试机当前可见的实际进程包括 `YYAnchor.exe`、`yyobs.exe` 和 SonoBusVideoCaptureHelper；可见模块查询报告 `SonoBusMoLiXiuHook.dll` 与 `molixiudll.dll` 均为 0 个匹配。该证据支持“当前生产者是 YYAnchor，而不是已确认兼容的 `molixiu.exe`”，但仍不能据此宣称 Hook 全面状态已完成验收。
- `SonoBusMoLiXiuBridge.exe` 的实现仍只寻找 `molixiu.exe`，并依赖固定的 MoLiXiu PE 布局、ABI 和 32 位注入；禁止仅把 `YYAnchor.exe` 加入目标列表。
- `dshow:@device_sw_{860BB310-5D01-11D0-BD3B-00A0C911CE86}\\yyanchorvcam` 已确认属于已知虚拟 DirectShow 设备。`63c1fae` 将 YYAnchor 的三个已知标识（`yyanchorvcam`、`yyanchormulvcam`、`yyanchormulcam`）与私有 MoLiXiu Hook 路由分开，避免仅因显示名含“魔力秀/YY开播”而改成 `--publish-molixiu`。
- `00ff309` 安装包仍是测试机当前部署版本。当前 `2de49cd` 包含 `63c1fae` 的 YYAnchor 路由、`f79d866` 的 MoLiXiu 控制/发布尝试改动以及混合大小写 selector 修复；对应的非 ASIO/ASIO Windows CI 已成功，但当前 artifact 尚未部署或完成 Windows 真机回归。不得把 CI、artifact 校验或本地单元测试结果当作远端部署完成证据。
- 最新一次可见 DirectShow 探测已进入 real-time buffer/frame dropped 路径，而不是此前的 `Could not find output pin`；这只证明探测边界有变化，仍需在新构建的直接 DirectShow 路由下确认持续递增帧和 RTSP 输出。
- 当前 `isKnownVirtualCamera()` 仍未改变识别规则；它负责 DirectShow 枚举过滤，而 `isMoLiXiuBridgeCamera()` 负责是否进入私有 Bridge。当前工作区已将后者改为所有大小写形式的 `dshow:` ID 先行 fail closed，再执行名称启发式；不要把这项防护延后到 publisher 参数阶段，也不要修改 `isKnownVirtualCamera()`，除非另有明确需求。

Hook 目前针对魔力秀 2.0.2111.2402 的固定二进制布局，并且故意对其他版本 fail closed。魔力秀升级后，需要重新确认时间戳、镜像大小、共享数据 RVA、回调偏移和像素结构。

### 服务端视频链路

- 客户端上传 H.264，服务器不重新编码，只做中转和 MediaMTX/RTSP/WebRTC 输出。
- 已增加 RTSP demuxer 超时、输入停滞/输出断开后的 muxer 重启，以及 muxer stdin EPIPE 处理。
- 相关提交：

~~~
a2b6b8a Pass client H264 through server muxer without re-encoding
b2cc2fc Restart group muxer when ingest stalls or output dies
fffcd30 Use RTSP demuxer socket timeout and survive muxer stdin EPIPE
~~~

服务器和后台的常规部署说明在：

~~~
docs/deployment.md
docs/video-relay.md
docs/download-and-use.md
~~~

## 3. 当前未完成/需要接手继续验证

当前最大的未闭环问题是：测试机已直接部署并核验非 ASIO `00ff309` v0.1.32 payload；当前 HEAD `2de49cd` 已推送且与远端同步，对应的非 ASIO/ASIO Windows CI、portable artifact manifest/hash/layout 和 installer payload 验证均已通过，但尚未部署。仍未完成 Windows 真机持续帧、RTSP/WebRTC、OBS、REAPER/VST3 和生命周期验收。不要把旧部署的结果归因于当前代码；重新部署仍需明确部署授权、回滚备份和部署后原始诊断证据。

当前工作区状态（Windows artifact 下载在 `/tmp`；源码改动已提交；本说明更新后自身会显示为修改）：

~~~text
git status --short --branch
## codex/dshow-drainer...origin/codex/dshow-drainer
M .scratch/h264-webrtc-video/issues/04-packaging-acceptance.md
M .scratch/h264-webrtc-video/issues/06-admin-camera-control.md
M .scratch/h264-webrtc-video/spec.md
M README.md
M docs/adr/0001-mediamtx-h264-control-plane.md
M docs/deployment.md
M docs/ffmpeg-runtime.md
M docs/video-relay.md
M packages/server/src/app.test.ts
M packages/server/src/app.ts
M sonobus/Source/VideoRelayClient.cpp
M sonobus/Source/VideoRelaySupport.cpp
M sonobus/Source/VideoRelaySupport.h
M sonobus/tests/VideoRelaySupportTest.cpp
M 接手说明.md
?? .scratch/uu-*.png
?? artifacts/
?? sonobus-macos-universal.pkg
?? sonobus-macos-universal.zip
?? sonobus/build-test/
~~~

`2de49cd` 及其之前的源码、测试和文档改动已提交并推送。上述 `.scratch/`、构建目录、安装包和 `artifacts/` 是已有本地产物，不要误删或误提交。Windows artifact 仅下载到 `/tmp/sonobus-artifacts-2de49cd-*`，未复制到仓库。

UU CLI 本次只形成了设备在线证据：设备 `aeawti5pauaujmkl`（`WIN-CUG15J16S85`）的 `device list`、`device info` 和 `echo` 成功，但 `term` 后 `device status` 持续为 `isConnecting:true`，可见 PowerShell 命令没有返回可采信的原始输出。已执行 `term exit --clear` 和 `device disconnect`，最终无活动连接；本次没有部署或修改测试机，不能作为 Windows 原生测试证据。

按以下顺序处理：

### A. 核验当前工作区并授权 Windows 真机部署

测试机：Windows 远程测试机 WIN-CUG15J16S85。

当前安装目录仍以已核验的 `00ff309` 非 ASIO payload 为基线。只有在当前工作区补丁审查完成、对应 ASIO/非 ASIO 完整产物、manifest/hash、provenance 和测试均通过，并取得明确部署授权后，才可替换它。替换前关闭：

- REAPER/其他 DAW
- SonoBus helper
- 旧版 ffmpeg.exe
- 旧版 SonoBusMoLiXiuBridge.exe

安装后检查以下两个 VST3 目录都存在新版文件：

~~~
C:\Program Files\Common Files\VST3\SonoBus.vst3\Contents\x86_64-win\
C:\Program Files\Common Files\VST3\SonoBusInstrument.vst3\Contents\x86_64-win\
~~~

至少确认：

~~~
SonoBusMoLiXiuBridge.exe
SonoBusMoLiXiuHook.dll
ffmpeg.exe
SonoBusVideoCaptureHelper.exe
~~~

验证新版桥不再接受旧的假成功行为：

~~~
& 'C:/Program Files/Common Files/VST3/SonoBusInstrument.vst3/Contents/x86_64-win/SonoBusMoLiXiuBridge.exe' --attach --dll 'C:/Windows/definitely-missing.dll'
$LASTEXITCODE
~~~

结果应为非零；旧版桥曾经对这个错误参数返回 0。

### B. 确认 Hook 真正注入

启动魔力秀和 DAW/VST 后检查：

~~~
tasklist /m SonoBusMoLiXiuHook.dll
Get-Content "$env:APPDATA\SonoBus\molixiu-camera.txt"
~~~

必须能看到 Hook 已加载到 molixiu.exe，并且设备文件内容会随魔力秀选择摄像头变化。设备名是用户可自定义的，不能在代码里写死 YY开播等显示名称，应使用设备标识/最近选择快照。

### C. 做真正的断开/重开验收

在魔力秀中依次测试：

1. 选择物理摄像头，确认 VST/后台/RTSP 有真实运动画面。
2. 关闭魔力秀摄像头，确认 SonoBus 仍能从最近选择的设备取得画面，或者在设备确实不可读时明确报告无帧。
3. 再次打开魔力秀摄像头，确认画面自动恢复，不需要重启 DAW。
4. 切换到 YY/其他虚拟摄像头，再切回物理摄像头。
5. 魔力秀播放视频文件、暂停视频、切回摄像头，确认魔力秀自己的输出不被抢占。
6. 同时让其他软件打开同一摄像头，确认 SonoBus fallback 会释放设备，不持续独占。

观察 helper 输出中的状态：

~~~
capture_mode=molixiu-hook
capture_mode=molixiu-direct  # 物理 SharedReadOnly fallback，不是 DirectShow
molixiu_source=hook
~~~

若只出现“等待魔力秀选择摄像头”，先查 Hook 是否加载和 molixiu-camera.txt 是否更新，不要先改服务器。

### D. 验证服务器实际有帧

客户端画面正常后，在服务器或能访问服务器的机器上用 FFmpeg 拉取 RTSP，至少持续 15–30 秒并确认帧数大于 0：

~~~
& 'C:/Program Files/Lossless Audio SonoBus Relay/ffmpeg.exe' -hide_banner -loglevel warning -rtsp_transport tcp -analyzeduration 10M -probesize 10M -i 'rtsp://<服务器地址>:19092/SB_<群组名>' -t 15 -map 0:v:0 -an -f null NUL
~~~

同时验证浏览器 /SB_<群组名> 和 OBS 的 RTSP 地址。若 FFmpeg 有帧而网页/OBS 无画面，再查 MediaMTX/WebRTC/浏览器；若 FFmpeg 也无帧，先查客户端 helper 日志和上行连接。

### E. 最后重新构建和发布（针对当前工作区；生成后记录实际 HEAD）

当前 GitHub Actions 和 artifact 状态（均对应 `2de49cd`）：

~~~text
非 ASIO：34609121870 — success，HEAD 2de49cdc
ASIO：34609127561 — success，HEAD 2de49cdc
非 ASIO artifact：`sonobus-windows-x64` / `sonobus-windows-x64-installer`，未过期
ASIO artifact：`sonobus-windows-x64-asio` / `sonobus-windows-x64-asio-installer`，未过期
artifact 下载目录：`/tmp/sonobus-artifacts-2de49cd-*`（未写入仓库）
~~~

独立核验结果：两套 portable ZIP 均可完整解压；`build-manifest.json` 的 `gitSha`、ref、workflow、run ID 均匹配，32 个文件的 SHA-256 全部重算一致；`SonoBus.exe`、helper、FFmpeg、VST3 为 x64，MoLiXiu Bridge/Hook 与 `ffmpeg32.exe` 为 x86。ZIP 长度/SHA-256：非 ASIO `249929888` / `201eeb532ad0c2b42beaec85664a4ea30cf50067a03c2628b937317cfcd25622`，ASIO `249987199` / `3bb033e6aac2ed4d1e97e8d00729d22b2054fa2aa9fe51bb520b8fbef9164a78`。安装器长度/SHA-256：非 ASIO `181633177` / `60d573cc125ec888c629b9a1585e4d6ce48ad289cbbcba4f1e678f606f7025d6`，ASIO `181665802` / `c21fec13a871b53905382e1f0b2d30db24cc787d68b319651b5f8fec92e4037e`。Windows CI 的 `Verify installer payload` 和卸载清理均成功；portable manifest 的 32 个 payload hash、架构和布局均通过；本机 `innoextract 1.9` 不支持该 Inno loader revision 2，因此不把本地解包工具错误当作 installer payload 失败。

这些证据只证明 `2de49cd` 的构建产物完整，不能视为已部署或真机视频链路验收完成。

本地基础检查：

~~~
cmake --build sonobus/build-test --target SonoBusVideoTests -j2
ctest --test-dir sonobus/build-test --output-on-failure
cmake --build sonobus/build-test --target SonoBus_Standalone SonoBus_VST3 -j2
npm run typecheck
npm test
npm run build
~~~

Windows 原生桥接和安装包必须通过 GitHub Actions：

~~~
.github/workflows/build-windows-asio.yml
.github/workflows/build-windows.yml
~~~

发布前核对：Actions 成功、tag 指向正确提交、Release 为 published、Windows ASIO 安装包非空且包含上述四类文件。不要只以“源码编译成功”作为完成依据。

## 4. 已完成的本地验证

本次接手后最后运行：

~~~text
cmake --build sonobus/build-test --target SonoBusVideoTests -j2
ctest --test-dir sonobus/build-test --output-on-failure
cmake --build sonobus/build-test --target SonoBus_Standalone SonoBus_VST3 -j2
npm run typecheck
npm test
npm run build
~~~

结果：`SonoBusVideoTests` 构建成功，CTest `1/1` 通过；macOS `SonoBus_Standalone` 与 `SonoBus_VST3` 构建成功；`npm run typecheck`、`npm test`（协议/服务器/relay state 共 `42/42`）和 `npm run build` 全部通过。macOS libc++ 输出的 “selected platform is no longer supported” 只是已有编译警告，不是测试失败。

这些是本地/便携测试，不是 Windows 真机验收；当前源码仍没有重新部署到测试机。

## 5. 关键约束

- 物理摄像头不得独占，必须使用 `SharedReadOnly`；虚拟摄像头只有在管理员明确选择时才允许走 DirectShow，且不得作为自动 fallback。
- fallback 只能短时读取，并且必须释放；其他软件和魔力秀应能重新取得设备。
- 不得因为魔力秀的显示名称变化而失效。
- 不要把视频重新编码当成默认修复方案；服务器目标是 H.264 中转。
- 普通管理员选择不得静默切换设备；MoLiXiu 专用 Hook fallback 如选择其他物理摄像头，必须记录并显示实际设备名。
- 不要把真实服务器密码、SSH 私钥、后台账号密码提交到 Git。

## 6. 接手时第一步

~~~
cd "/Volumes/存储/Applications/ls/SonoBus无损音频中继"
git status --short --branch
git log -5 --oneline
~~~

先确认 `00ff309` 当前部署基线与当前 `2de49cd` 提交严格区分；当前两套 Windows CI 已覆盖 `2de49cd`，artifact 的 provenance/hash、架构和安装布局已独立核验，但测试机仍未替换。只有取得明确部署授权后，才允许使用新 artifact 直接复制并保留回滚备份；随后必须取得 Windows 原始诊断、精确 selector、Helper/FFmpeg 参数、持续帧、RTSP/WebRTC/OBS 证据，避免把旧安装包问题误判成新代码问题。

